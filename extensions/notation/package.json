{
  "main": "dist/extension.cjs",
  "scripts": {
    "build": "node esbuild.js",
    "build:prod": "node esbuild.js --production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm test",
    "package": "npm run build:prod && node ../../extensions-sdk/package.cjs ."
  },
  "dependencies": {
    "@ableton/extensions-sdk": "file:../../extensions-sdk",
    "opensheetmusicdisplay": "^1.8.9",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "typescript": "^5.6.3",
    "vitest": "^4.1.4"
  }
}
